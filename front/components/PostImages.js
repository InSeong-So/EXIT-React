import React from 'react'
import PropTypes from 'prop-types'

const PostImages = ({ images }) => {
  return (
    <div>
      구현하고 있어용
    </div>
  )
}

PostImages.PropTypes = {
  images: PropTypes.arrayOf(PropTypes.obejct),
}

export default PostImages